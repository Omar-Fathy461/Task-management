import './spinner.scss'

const Spinner = () => {
    return (
        <div className="spinner">
            <div className="loader"></div>
            <h1>Loading...</h1>
        </div>
    );
};

export default Spinner;
